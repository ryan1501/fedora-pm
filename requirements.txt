# Fedora Package Manager Requirements
# 
# CLI tool (fedora-pm.py):
# - Uses only Python standard library
# - No external Python dependencies required
#
# GUI tool (fedora-pm-gui.py):
# - PySide6 (Qt for Python) - Required for the GUI front-end
#
# System requirements:
# - dnf (Fedora package manager)
# - rpm (RPM package manager)
# - sudo access for package operations

# GUI dependencies
PySide6>=6.0.0

